import { getAnimatingState } from "./fake-animation-library";

//so here we won't be able to edit the above function ts as we are assuming that this fn will be exported from the external library

const animatingState = getAnimatingState();
